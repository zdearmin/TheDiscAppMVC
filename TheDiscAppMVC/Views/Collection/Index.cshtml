@model IEnumerable<TheDiscAppMVC.Models.Collection.CollectionListItem>

@{
    ViewData["Title"] = "Index";
}

<h1>Collection Index</h1>

<p>
    <a asp-action="Create">Add a new collection</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NumOfPlayers)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NumOfDiscs)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumOfPlayers)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumOfDiscs)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
}
    </tbody>
</table>
